{
  "name": "bankv1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bankv1",
    "create-local": "graph create --node http://localhost:8020/ bankv1",
    "remove-local": "graph remove --node http://localhost:8020/ bankv1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bankv1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
